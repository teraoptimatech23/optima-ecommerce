# Fallback Route

Every unknown route must render the global NotFoundPage.

Example

/abc

↓

NotFoundPage

The NotFoundPage should:

- Hide Bottom Navigation.
- Display the Empty Page design.
- Provide a "Back to Home" button.
- Never display a blank page or browser error.